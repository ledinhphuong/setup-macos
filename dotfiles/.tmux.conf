#
# Copyright (c) 2019 Le Dinh Phuong

set -g base-index 1
setw -g pane-base-index 1
set -g mouse on

# Unbind default prefix and set it to ctrl+a
set -g prefix C-a

# Bind hotkeys for splitting panes
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Turn on true color
set -g default-terminal 'screen-256color'
set -ga terminal-overrides ",xterm-256color*:Tc"

# Set scrollback buffer to 10000
set -g history-limit 10000

# Set inactive/active window styles
set-window-option -g window-style bg=colour236
set-window-option -g window-active-style bg=black

# Customize the status line
#set -g status-fg  green
#set -g status-bg  black
#
#set -g status-right-length 150
#set -g status-right "%H:%M %b %d,%y"
#
#set -g window-status-format '#(pwd="#{pane_current_path}"; echo ${pwd####*/})#F'
#set -g window-status-current-format '#(pwd="#{pane_current_path}"; echo ${pwd####*/})#F'
#
## status bar updates every 15s by default**, change to 1s here
## (this step is optional - a lower latency might have negative battery/cpu usage impacts)
#set -g status-interval 1

# Basic status bar colors
set -g status-bg "#2d323b"
set -g status-fg white

# Left side of status bar
set -g status-left bg=black,fg=colour243
set -g status-left-length 40
set -g status-left "#[fg=black,bg=#3a783c,nobold] #S #[fg=#3a783c,bg=#3f4451]#[fg=colour247,bg=#3f4451] pane #P/#{window_panes} #[fg=#3f4451,bg=#2d323b]"

# Right side of status bar
set -g status-right bg=black,fg=colour243
set -g status-right-length 150
set -g status-right "#[fg=#3f4451,bg=#2d323b]#[fg=colour247,bg=#3f4451] %a, %D #[fg=#697a82,bg=#3f4451]#[fg=black,bg=#697a82] Powered by: phuongle #[fg=black,bg=#697a82]"

# Window status
set -g window-status-current-format "#[fg=#2d323b,bg=#245f83]#[fg=colour247,nobold] #{pane_current_path} #[fg=#2d323b,bg=#245f83,nobold]"

# Window status alignment
set -g status-justify centre

# Pane border
set -g pane-border-style fg=white
set -g pane-active-border-style bg=default,fg=green

# Pane number indicator
set -g display-panes-colour white
set -g display-panes-active-colour brightred

# Clock mode
set -g clock-mode-colour white
set -g clock-mode-style 24

# Status bar updates every 15s by default, change to 1s here
# (this step is optional - a lower latency might have negative battery/cpu usage impacts)
set -g status-interval 1
